module.exports = {
  plugins: {
    // Fixme (Nour): enabling allows RTL display, but smoehow removes the sidebar... Needs more investigation
    // 'postcss-rtlcss': { /* plugin options */ },
  },
}
